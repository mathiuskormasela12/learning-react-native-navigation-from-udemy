import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  page: {
    flex: 1
  },
  wrapper: {
    flex: 1,
    padding: 16
  },
  flatList: {
    flex: 1
  }
})

export default styles
